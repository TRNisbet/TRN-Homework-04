var displayObject = JSON.parse(localStorage.getItem("userHighScore"));

for (let index = 0; index < displayObject.length; index++) {
    const element = displayObject[index];
    console.log('element:', element)
//    need if statement ti stop the array if it is empty
// create list item - element.name element.score - she sent a element sort - 
}